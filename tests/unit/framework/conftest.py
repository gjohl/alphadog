import pytest

from tests.mock.mock_config import (
    mock_instrument_config, mock_instrument_dict, mock_instrument,
    mock_strategy_config, mock_strategy_dict, mock_instrument_weights_config
)
from tests.mock.mock_data import (
    mock_price, mock_ohlcv, mock_fx_rate, mock_trading_capital, mock_forecast_signal,
    mock_signal_list
)
